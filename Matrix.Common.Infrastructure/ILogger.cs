using System;
using System.Collections.Generic;
using Matrix.Domain.Entities;
namespace Matrix.Common.Infrastructure
{
    public interface ILogger
    {
        //event EventHandler<LogReceivedEventArgs> LogReceived;
        ///// <summary>
        ///// Попытка записать в лог
        ///// </summary>
        //event EventHandler<LogReceivedEventArgs> LogWritten;
        //void NeedLogBy(System.Collections.Generic.IEnumerable<Guid> objectIds);
        //void NeedLogBy(Guid objectId);
        //void NoMoreNeedLogBy(System.Collections.Generic.IEnumerable<Guid> objectIds);
        //void NoMoreNeedLogBy(Guid objectId);
        //void RegisterRules();
        //void WriteLog(Guid associatedObjectId, string text);
        //Guid BeginStore(Guid relatedObjectId, Guid initiator);
        //DataRecord EndStore(Guid key);
    }
}
